spring:
  application:
    name: marketing-redirect-service

  datasource:
    url: jdbc:mysql://localhost:3306/redirect_db
    username: redirect_user
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

